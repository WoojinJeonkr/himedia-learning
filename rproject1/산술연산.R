# 변수에 2개의 값을 넣고
a <- -6
b <- 9

# 1. 사칙연산
print(a + b)
print(a - b)
print(a * b)
print(a / b)

# 2. 더한 결과값의 절대값과 루트값
print(abs(a + b))
print(sqrt(a + b))

# 3. 나머지
print(a %% b)

# 명령어 사용
# packages 설치 : cowsay
install.packages("cowsay")